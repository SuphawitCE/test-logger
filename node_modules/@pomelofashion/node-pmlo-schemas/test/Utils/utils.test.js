const { Utils } = require('../../src');

test('should expose validStores and validLanguages arrays from Utils', () => {
  expect(Utils).toHaveProperty('validStores');
  expect(Utils).toHaveProperty('validLanguages');
});
